#!/usr/bin/env python3
"""module that concatenate two strings"""


def concat(str1: str, str2: str) -> str:
    """function that returns two concatenated strings"""
    result = str1 + str2
    return result
